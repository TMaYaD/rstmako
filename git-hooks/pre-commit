#!/bin/bash

TEMPLATE_DIR=templates
PUBLIC_DIR=public

for template in `git diff-index --name-only HEAD --relative=${TEMPLATE_DIR}`
do
	ifile=${TEMPLATE_DIR}${template}
	ofile=${PUBLIC_DIR}${template/%mako/html}
	python scripts/render.py ${ifile} > ${ofile}
	git add ${ofile}
done
